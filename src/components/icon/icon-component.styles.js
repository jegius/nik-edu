export default `
    <style>
        @import url('../common.css');

        .icon_logo {
            background: var(--logo) no-repeat;
        }

        .icon_header {
            background-size: 1.75rem;
            min-width: 1.75rem;
            min-height: 1.75rem;
        }

    </style>
`;