export default `
    <style>
        @import url('../common.css');

        body {
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 9rem;
            background-color: var(--bg);
            color: var(--white);
        }

        .main-site {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 9rem;
            background-color: var(--bg);
            color: var(--white);
            overflow-x: hidden;
        }

    </style>
`;

