export default `
    <style>
        @import url('../common.css');

        .main-site {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 9rem;
            background-color: var(--bg);
            color: var(--white);
            overflow-x: hidden;
        }

    </style>
`;

